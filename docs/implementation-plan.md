# 萌宠小镇数学冒险：实施计划

更新日期：2026-07-27

## 1. 产品基线

- 目标用户：上海小学一年级儿童，包含尚不能独立阅读的孩子。
- 首期学科：数学。
- 教材范围：上海教育出版社五四学制一年级上册；当前仅依据公开单元结构规划，精确知识点必须经过目录或教材内页复核。
- 首期知识域：`10以内的数`；扩展路线包含 10 以内加减法、20 以内进位加法与退位减法。
- 游戏主题：萌宠小镇冒险。
- 首发平台：Web、Android APK；微信小游戏作为首版稳定后的独立可行性验证。
- 引擎：Godot 4.7，2D，1280×720 横屏。
- 核心成功标准：孩子无需识字也能理解操作，并愿意主动重玩。

## 2. 产品原则

1. 一次只给一个行动指令，语音可随时重播，并配合视觉高亮。
2. 答错不扣分、不掉生命，不播放羞辱或惊吓反馈；只提供更短的提示。
3. 教学内容由 Codex 直接设计和维护，不在游戏运行时接入任何大模型 API。
4. MiniMax 仅在开发阶段离线生成 TTS；API Key 不进入 Git、Web 或 APK。
5. Bitto 用于原创角色与场景美术；可计数物件使用独立精灵组合，不用 AI 生成固定数量的物件组。
6. 不复制教材扫描页、题目原文或教材插画。
7. 小主人默认称呼为“小宝贝”；内置“香香”语音。显示名与名字音频分离，为后续家长输入自定义名字保留接口。
8. TTS 采用混合拼接：故事与情绪台词保留整句；小主人名字、角色名、数字、数量词和运算符作为可复用片段。所有对白中出现的可替换名称都必须独立播放，不得录死在固定句段中。
9. 拼接优先使用自然短句而不是逐词串联：名称保持独立，数量与前后名词、动作一起朗读，固定问题单独复用；数字与运算符单词库主要服务后续十格策略的分步讲解。

## 3. 里程碑

### M0：工程基线（已完成）

- [x] 确定 Godot、目标分辨率和首发平台。
- [x] 确定首个知识域与三个小游戏方向。
- [x] 建立 Git 仓库与项目内实施计划。
- [x] 建立 Godot 主场景、自动加载服务、JSON 内容与本地存档。
- [x] 建立无密钥的音频注册表与 Web/Android 导出预设。

验收：Godot 可无报错启动，内容能够从 JSON 加载，进度能保存到 `user://progress.json`。

### M1：数数配餐可玩切片

- [x] 生成 1–10 的目标数量，首轮从 1–5 开始，后续扩展到 10。
- [x] 点击胡萝卜放入篮子；点击篮中胡萝卜可取回。
- [x] 使用明确的确认按钮提交答案。
- [x] 答错只提示“再数一数”，不结束回合。
- [x] 完成五个回合后给予庆祝并允许一键重玩。
- [x] 提供语音重播按钮与视觉提示占位。

验收：只使用程序绘制的占位美术也能完整玩一局；鼠标、触摸和键盘均可操作；无死局。

### M2：内容与自动验证

- [x] JSON 驱动题目、提示语与难度参数。
- [x] 确定性随机序列，避免连续重复。
- [x] 内容结构校验、出题逻辑测试、存档读写测试。
- [x] 记录完成局数和累计完成回合，不记录儿童身份信息。

验收：无效内容在启动或测试时明确报错；固定种子可复现题目序列。

### M3：语音定版

- [x] 定稿 1–10 完整指令、短版待机提示、重试与奖励台词清单。
- [x] 建立三音色试听清单和 Godot 音频注册表。
- [x] 将小主人名字拆成独立音频片段，内置“小宝贝”和“香香”，自定义名字无音频时回退到默认称呼。
- [x] 生成“小宝贝”和“香香”的独立 MiniMax Ogg 试听。
- [x] 在 MiniMax 控制台轮换已经暴露的订阅 Key。
- [x] 将新 Key 放入被忽略的 `.env.local`，权限设为 `600`。
- [x] 使用 `$minimax-tts` 完成温柔女声实机试听，并根据家长反馈修订引导文案。
- [x] 使用默认旁白、导师和奖励音色，批量生成 25 条 Ogg Vorbis。
- [ ] 通过实机试玩最终确认旁白、导师和奖励音色。
- [ ] 逐类试听指令，检查数字、多音字、停顿和语速。

验收：核心流程不依赖阅读；所有指令均可重播，并有短版待机提示。

### M4：剧情序章与小镇选关

- [x] 选定柔和绘本风作为剧情方向，保留底部对白和大按钮空间。
- [x] 实现四句胡萝卜野餐剧情，支持语音重播、进度提示和跳过。
- [x] 剧情结束后再进入“数数配餐”，不再启动即做题。
- [x] 建立小镇选关页，展示数数、加法、减法和十格策略路线。

验收：孩子能通过图画、语音和大按钮理解故事任务，不需要独立阅读。

### M5：加减法与十格策略

- [x] 实现 10 以内“合起来”加法和“拿走了”减法，以两组实物同步呈现算式。
- [x] 实现 20 以内凑十法：拆小数、补成十、再加余数。
- [x] 实现 20 以内破十法：拆大数、从十里减、再加余数。
- [ ] 实现平十法：拆减数、先减到十、再减余数。
- [ ] 实现借十法挑战关：两位数减一位数，不标记为当前已核实的一年级上册主线。
- [x] 建立小主人名、角色名、零到四十、数量词和运算符的可拼接 TTS 词库。
- [ ] 为每种策略建立 JSON 题库、低难度引导、温和重试和自动验证。

验收：每一步拆分都能在十格篮子中看到、点到和听到；不只背口诀。

### M6：原创美术与体验完善

- [x] 使用 Godot 4.7 Offset Transform 为通用按钮、选关卡片和胡萝卜按钮提供悬浮与按压反馈。
- [ ] 使用 Bitto 完成米米兔、点点猫、团团熊和萌宠小镇正式美术。
- [ ] 检查轮廓、对比度、表情、UI 留白和角色一致性。
- [ ] 接入角色动画、粒子、音效和积极反馈。
- [ ] 扩展“数字排队”和“多一点、少一点”。

验收：目标物件容易辨认和计数；反馈动画不遮挡题目或诱导误触。

### M7：教材校验与发布

- [ ] 根据教材目录或内页核对知识点边界和术语。
- [ ] 未经确认的“第几个”、数轴等内容保持禁用。
- [ ] 验证 Web 导出、Android APK、横屏适配、触摸、中文字体、音频和离线运行。
- [ ] 首版稳定后评估微信小游戏适配成本与风险。

验收：Web 和 APK 可在目标设备离线完成整局；项目不包含 API Key、教材扫描件或未经授权素材。

## 6. 执行记录

### 2026-07-27：首个可玩切片

- 完成 Godot 工程、课程 JSON、自动加载服务、本地匿名存档和导出预设。
- 完成“数数配餐”五回合玩法、温和重试、完成庆祝和重玩流程。
- 自动检查通过 58 项，覆盖课程范围、确定性出题、连续去重、存档和完整五回合流程。
- 1280×720 初始态与十根胡萝卜极限状态完成截图检查，无布局溢出。
- Web Debug 导出和签名 Android Debug APK 均已成功；真机触摸与设备音频留在发布里程碑继续验证。

### 2026-07-27：语音生产准备

- 定稿 25 条首版游戏语音：十条完整目标、十条短提醒、重试、三条正确奖励和完成奖励。
- 建立三种候选旁白的同文案试听清单，真实生成等待轮换后的订阅 Key。
- 游戏加入九秒无操作短提示；每次点击、取回或重播都会重新计时。
- 新增小主人本地显示名：默认“小宝贝”，内置“香香”，最长八个字符；名字音频与游戏指令可顺序拼接。
- 生成个性化语音首批 3 段 Ogg 试听，共计 33 个计费字符；其中旧版欢迎语已根据试听反馈移除。
- 将完整题目改为“米米想要 X 个胡萝卜。你可以帮我把胡萝卜放在篮子里吗？”，第一题前独立播放小主人名字。
- 批量生成 25 条正式语音，共计 738 个计费字符；完整指令时长 4.83–5.35 秒，短提示 2.17–2.37 秒。
- 本机试玩存档单独设为“香香”，不进入 Git。

### 2026-07-27：剧情序章与加减法扩展

- 明确两张 Bitto 图为美术方向稿；选用第一张柔和绘本风作为剧情序章背景。
- 完成团团熊、米米兔和点点猫的四句对话，第一句通过独立名字音频称呼小主人。
- 生成 4 段剧情 Ogg 语音，共计 155 个计费字符。
- 确定凑十法、破十法、平十法与借十法的分层路线；借十法暂作挑战关。
- 确定 TTS 混合拼接方案：名字、角色、数字和运算符作为可复用片段，情绪对白保留整句。
- 新增四路线小镇选关页与 30 字符语音引导；数数配餐支持返回小镇，未接入的路线明确锁定。

### 2026-07-27：10 以内加减法与可复用语音

- 完成“合起来”和“拿走了”两条五回合路线：两组胡萝卜、同步算式、三个数字点阵答案、温和重试、完成庆祝与返回小镇。
- 重做答案卡片对比度：使用深橙大数字、深绿点阵、奶油底和清晰描边，数字字号提升到 58。
- 将剧情小主人名统一为 `$child_name` 音频占位符；数数与加减法对白中的米米、点点等角色名全部改为独立片段。
- 建立 58 个共享语音片段：三个角色名、1–10 数量词、0–40 数字、三个运算符和“胡萝卜”；新增数数固定句段与加减法固定句段。
- 序列播放器自动跳过短片段头尾多余静音，保留自然短停顿；移除 10 条目标整句、10 条短提示和一条录死角色名的旧语音及其导入文件。
- 使用 Bitto `gpt-image-2` 自动路由生成原创透明底水彩胡萝卜图标；“拿走”状态改为淡原色轮廓加珊瑚色减号，不再使用灰脏滤镜。

### 2026-07-27：拼接语音自然度优化

- 根据实机试听反馈，放弃名称、动词、数量词和名词逐词串联；名称继续独立，其他内容合并为带完整语调的自然短句。
- 数量继续保留在“想要三个胡萝卜”“又送来三个胡萝卜”一类自然短句中；数数、加法和减法的固定问句各只保留一份。
- 无操作提醒复用当回合“米米 + 数量短句”，不再为 1–10 另生成十条提醒语音。
- 新增“米米、”“点点、”承接语气名称，片段衔接的头尾静音分别裁剪 0.30 秒和 0.33 秒，实测名称后的空白约为 0.11 秒。
- 第一版自然短句共生成 50 条、使用 1,310 个计费字符；试听后进一步改为可线性扩展的短句复用结构，避免后续为每个数字重复生成整段问题。
- 精简版仅补生成 31 条短片段、使用 497 个计费字符；最终有效语音由中间版 132 条降至 118 条，`audio/tts` 由约 3.9 MB 降至 2.9 MB。

### 2026-07-27：20 以内凑十小桥

- 新增 20 道受控凑十题，一局五回合；每题都验证“补成十、拆小数、十加余数”，相邻题避免重复答案。
- 完成两步可视化玩法：先从点点一侧拿胡萝卜补满米米的十格篮子，再从三个大数字中选出 `10 + 余数` 的结果；移入的胡萝卜可以撤回。
- 答案卡片取消重复圆点，只保留居中的大数字；补满阶段确保十个格子实际显示十根胡萝卜。
- 新增 10 条凑十法语音，共 235 个计费字符；复用角色、数量、数字和运算符片段，答对后完整朗读 `10 + 余数 = 答案`。
- 小镇选关卡片加入四张原创水彩图标；加减法卡片使用概念符号，避免图中数量与示例算式不一致。
- 课程范围继续标记为 `scope-pending-textbook-page-verification`，在获得教材内页前不声称页码级对齐。
- Godot 导入与 925 项自动检查通过；1280×720 的补十、答题阶段无溢出，Web Debug 导出成功且不包含本地密钥配置。

### 2026-07-27：破十山洞与通用按钮动效

- 完成 20 以内破十法五回合路线：孩子先从十格篮子中拿走减数，再把十以内的剩余和个位胡萝卜合起来选择答案。
- 新增一个可复用的 `OffsetTransformButtonMotion` 组件；所有通过 `UIStyles` 创建的按钮自动获得悬浮抬起和按压下沉，选关卡片与胡萝卜按钮使用各自的反馈强度。
- 动效使用 Godot 4.7 的视觉偏移、中心缩放与 `visual_only` 点击热区保护；重复应用按钮样式不会重复挂载组件或连接信号。
- Godot 4.7 导入与 1071 项自动检查通过。

## 4. 技术结构

- `ContentRepository`：加载并校验课程 JSON。
- `CountQuestionGenerator`：生成可复现的数量题目序列。
- `ProgressStore`：保存匿名本地进度。
- `AudioManager`：播放预生成语音和音效，缺少素材时安全降级。
- `OffsetTransformButtonMotion`：集中管理按钮、卡片和可点击物件的 Godot 4.7 Offset Transform 反馈。
- `SceneRouter`：管理场景切换。
- `content/`：教材对齐状态、题目参数和提示语。
- `audio/tts/`：预生成语音与非敏感音频注册表。

## 5. 当前风险与处理

| 风险 | 处理方式 |
| --- | --- |
| 只有教材封面，无法核对具体页面 | 先做公开单元范围内的基础数概念，设置教材校验门 |
| 幼儿不识字 | 语音优先、图形操作、重播按钮、短指令 |
| Godot 微信小游戏支持不如 Web/APK 直接 | 首版后单独做技术验证，不阻塞首个可玩版本 |
| API Key 曾出现在聊天 | 不写入项目；生成语音前必须轮换 |
| AI 美术出现计数错误 | 单个物件独立生成与组合，数量由游戏逻辑控制 |
| 四种口算策略的年级边界尚未全部核实 | 分层解锁；借十法作挑战关，获得教材内页前不标注为上册必学 |
